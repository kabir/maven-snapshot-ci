# Will be created by quick.yml as a quick and dirty way to make sure it all works
name: CI

on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          # The personal access token must be something which has the workflow permissions
          # (I also used repo, admin:repo_hook and user)
          token: ${{ secrets.CI_PAT }}

      - name: Say hello
        run: |
          echo "The created job works"